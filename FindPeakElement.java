class FindPeakElement {
	/*
	brute
	for (i=0 ; i < n ; i++) {
		if ((i == 0 || arr[i-1] < arr[i]) && (i == n-1 || arr[i] > arr[i+1])) {
			return i;
		}
	}
	*/
	public static void main(String[] args) {
		
	}
}